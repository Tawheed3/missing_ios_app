name: missing_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6
  provider: ^6.1.5+1
  cached_network_image: ^3.4.1
  intl: ^0.20.2
  image_picker: ^1.2.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  url_launcher: ^6.3.2
  video_player: ^2.11.0
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^6.1.0
  sign_in_with_apple: ^6.1.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/app_photo.jpeg

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/app_photo.jpeg"